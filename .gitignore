node_modules/
miniprogram_npm/
.DS_Store
Thumbs.db
*.log
cloudfunctions/*/node_modules/
cloudfunctions/*/package-lock.json
project.private.config.json
.headroom-cache/

# 本地密钥/配置（避免误提交）
.env
.env.*
!.env.example
.mcp.json

# 工具缓存
.codegraph/
.trae/.tmp/
codebuddy-plugin/
# R3v2 审计 #1：.codebuddy 工具数据目录含真实密钥（models.json），整体停止跟踪
# 2026-09-11 冗余扫描处置：改写为 .codebuddy/* —— 整体忽略【目录】时，下行的 ! 例外不生效
# （git 不会进入被忽略的目录去检查例外），改为忽略【子项】后例外才能放行项目自有的手写资产。
.codebuddy/*
# 例外：项目自有手写规则/技能（不可再生，必须入库）；工具安装的官方规则包/技能镜像仍被忽略。
# ⚠️ git 硬规则：父目录被排除时无法再包含其内容 → 必须【逐级】放行，三步缺一不可：
#   ① 放行路径上的目录本身 ② 重新忽略该目录的内容 ③ 放行目标子目录
!.codebuddy/rules/
!.codebuddy/skills/
.codebuddy/rules/*
!.codebuddy/rules/cloud-rules/
!.codebuddy/rules/project-rules/
.codebuddy/skills/*
!.codebuddy/skills/quality-gate/
!.codebuddy/skills/dsh-review/
!.codebuddy/skills/product-clause-import/
# 但该技能的运行产物目录不入库（每次运行都会变）；fixtures/ 样本保留以支持离线验证
.codebuddy/skills/product-clause-import/out/

# 测试输出
test-output.txt
jest.log
sync.log
coverage/
scripts/dbg*.txt

# 云函数部署配置：reportAI 段落写有真实 DEEPSEEK_API_KEY，整体停止跟踪（处理同 .codebuddy）
# 恢复方式（灾难重建）：复制 cloudbaserc.template.json → cloudbaserc.json，填入真实 key。
# key 可从云端取回：queryFunctions getFunctionDetail 的 Environment.Variables（reportAI）。
# 入库的 template 永不含密钥，是这份配置的"可恢复锚点"。
cloudbaserc.json

# reportPdf 中文渲染字体 8MB 二进制，不入库；克隆后从字体源恢复到 cloudfunctions/reportPdf/fonts/
cloudfunctions/reportPdf/fonts/

# 本机工作笔记（历史记录已迁至 docs/worklog/ 入库；此规则仅防止新笔记误入库）
.workbuddy/memory/
.trae/audit/*.html

# AI-IDE 工具产物（2026-09-11 项目整洁治理）：多个 AI 编码工具各自安装规则包/技能包留下的
# 内容副本与指令文件（9 份同源），以及工具本机配置。均由工具按需重建，非项目资产。
# 移出跟踪不删本地文件；项目自有的规范与技能走 .codebuddy/ 例外（见上方）。
.agent/
.clinerules/
.cursor/
.kiro/
.qoder/
.windsurf/
.trae/
.comate/
.roo/
.lingma/
.claude/skills/
.claude/settings.local.json
.workbuddy/mcp.json
# 各工具安装的通用规则/指令文档（rules/ = CloudBase 官方规则包，26 主题）
rules/
.rules/
.gemini/
.qwen/
.github/copilot-instructions.md
# 各 IDE 的 MCP 接入配置（CloudBase AI ToolKit 生成，env.INTEGRATION_IDE 标明来源 IDE）
.codex/
.iflow/
.vscode/mcp.json
.vscode/settings.json

# 历史审计/扫描产物（一次性；结论已落进代码、质量门错误库与提交历史）
audit-report.html
ui-audit-report.html
/audit-full-*.html
/redundancy-scan-*.html
