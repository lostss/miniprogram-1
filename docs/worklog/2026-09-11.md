# 2026-09-11

## 项目全局理解（首次系统性通读）

目标：理解 miniprogram-1（保小秘）现状，为后续迭代建立上下文。

**通读路径（可复用）**：CONTEXT.md（产品/架构/数据模型）→ AGENTS.md（沟通与技能路由）→ 目录结构 → package.json/cloudbaserc.json → jest 全量跑绿确认健康度 → app.js + utils/apiClient.js（前端 API 路由真相源）→ 各云函数 index.js 头部。

**现状快照**
- 测试：86 suites / 955 tests 全绿（`npx jest --silent`，约 47s）；`npm test` 会先跑 `sync-shared.js --check`。
- 分支 master，存在 249 项未提交改动（含大量 cloudfunctions/<fn>/_shared 副本同步产物）+ 新增未跟踪项（cloudfunctions/reportPdf/、specs/、多个新 utils 与 tests）。
- 云环境 `cloud1-3gan2ae3d3b400f1`（app.js 硬编码 + cloudbaserc.json 一致）。

**架构要点**
- 小程序原生 3 页（index/report/clients）；report/index.js 868 行是绝对主战场，report 页承载 7 章基础报告 + 深度分析 + 自动重算 + 重试 + 编辑/孤儿现价关联。
- 前端所有云调用经 `miniprogram/utils/apiClient.js` 单一路由表（DIRECT_FN），返回归一化 `{ok,code,msg,data,throttled,needsConfirm,...}`；写操作强制 retries:0 防双写。
- 8 个云函数：dataQuery / dataWrite / conversationAI / reportAI / ocrService / login / cleanup / reportPdf。共享模块主源 `cloudfunctions/_shared/*`，靠 `scripts/sync-shared.js` 下发副本（规则：改主源不改副本）。
- 报告双轨：基础版 7 章纯规则引擎（miniprogram/utils/report/*：gap-engine / timeline-builder / chapter-builder）；深度分析走 reportAI（hy3 + DeepSeek 直连备选），30s CAS 节流自动触发 + 手动按钮。
- reportPdf 为新增服务端 pdfkit 导出（输入 familyId，输出 fileID），未在 cloudbaserc 之外单独文档化。

**待办/风险线索**：249 项未提交改动未审阅；specs/ 三份（deep-module-refactor / living-report / report-unification）未纳入 git；`.codebuddy` 曾被移出跟踪（历史提交提到 models.json 含泄露密钥）。

## Git 提交：250 项改动落地为单 commit（5bb604b）

用户决策（提交前逐项确认）：cloudbaserc.json 不入库 / 字体与 poc 都不入库 / .workbuddy/memory 与 .trae/audit HTML 不入库 / 单 commit。

**提交内容**：268 files changed, +14503 -6848。主线四条——① 活报告模型：深度分析改 `source:'auto'` 事件驱动（30s CAS 节流），readiness BLOCK 收敛为事实层，待澄清项落 `families.pending_clarifications`；② conversationAI undo（undo-handler / undo-store，工具路由 13→14）；③ 新增 reportPdf 云函数（pdfkit + NotoSansSC）；④ 前端纯函数拆分（readiness / report-analysis / report-share / domain-writes / role-infer / calc-age / md-parse）。

**.gitignore 新增**（写在文件末尾，风格沿用历史 commit 1251fe8 的注释说明）：`cloudbaserc.json`、`cloudfunctions/reportPdf/fonts/`、`scripts/poc/`、`.workbuddy/memory/`、`.trae/audit/*.html`。

**操作顺序（可复用）**：`git rm --cached <含密文件>` → 写 .gitignore → `git add -A` → `git diff --cached | grep '^\+.*sk-'` 复查 → 跑 jest → `git commit -F -`（heredoc）。

**遗留风险（未处理，需用户决定）**
- 本地 `cloudbaserc.json` 仍明文写着真实 DeepSeek key `sk-f435...`，虽然已不入 git，但本机磁盘仍有，建议轮换或移到环境变量。
- cloudbaserc.json 不再版本化后，8 个函数的 timeout/installDependency 配置失去共享真相源；若协作需恢复，应另建 `cloudbaserc.example.json`（当前未建）。
- `cloudfunctions/reportPdf/fonts/NotoSansSC-Regular.otf`（8MB）未入库，克隆后必须自行恢复，否则 reportPdf 本地跑不起来。
- 提交用了一次 `git commit --amend` 修 message 串行语句（未 push，安全），后续应直接在首次提交前校验措辞。

## 全面审计（六维）：产出 audit-full-20260911.html

基线 `5bb604b` + 3 项未提交修复（priority-read 静默吞错 + login/cleanup）。结论：健康，无 P0。

**核心结论**
- 测试 86 suites / 956 tests 全绿；全项目零 `any` 违规；`_shared` 零漂移。
- **覆盖率去稀释口径**：整体 52.55% stmts / 55.63% lines。关键教训——`jest --coverage` 默认会把 `cloudfunctions/<fn>/_shared/**` 副本一起计入，导致 conversationAI 被误读为 13%（实际 64%）。**正确做法**：`--collectCoverageFrom` 显式指定主源 + `--collectCoverageFrom='!cloudfunctions/*/_shared/**'`（注意该否定 pattern 在部分版本不生效，需逐目录 collectCoverageFrom）。
- 分布：`_shared/domain` 100% · login 100% · utils/report 93.7% · dataWrite 70.4% · reportAI 67.5% · ocrService 65.5% · conversationAI 65.1% · _shared 主源 55.9% · utils 54.9% · dataQuery 51.4% · **cleanup 9.8%（最低）**。

**P1 发现（3）**
1. **静默吞错同型残留 4 处**（根因类别未收敛）：`dataQuery/entity-query.js:99`（保单查询失败→`{data:[]}`，与已修的 policy-read 事故同型）、`dataQuery/family-list.js:92`（成员数静默变 0）、`dataQuery/message-query.js:23`（对话历史静默空）、`dataWrite/cash-value-matcher.js:63,76`。约定应为"读失败不返回空集"。
2. **CONTEXT.md 自相矛盾**：L34/L52 说深度分析已恢复自动触发（正确），L116/L69 仍写"已停止自动触发，待手工入口"（上一版遗留）。
3. **`.codebuddy/rules/cloud-rules/RULE.mdc` L19/L25 描述已废弃架构**：仍写「前端 A 通道 `wx.cloud.extend.AI...streamText` 直调 + 双通道 + 绕过审查」。代码事实：前端 `wx.cloud.extend.AI` 已完全移除，对话单通道经 conversationAI，审查链完整——规则未跟上，属正向演进未同步。

**P2 发现（5）**：cleanup 覆盖率 9.8%（历史 bug 类别所在，需补 TTL_COLS 映射测试）· cloudbaserc.json 去版本化致配置无共享真相源（建议补 example）· 无 CI/pre-commit 守护 `sync-shared --check` · reportPdf 产物 `reports/<openid>/<familyId>_<ts>.pdf` 无清理策略会累积 · 本机 cloudbaserc.json 明文 DeepSeek key 未轮换。

**P3**：`insights` 集合疑似僵尸（仅读取引用、无写入方，红点逻辑已由 families.last_analysis + insight_stale 承载）· dataQuery 覆盖率最弱。

**已验证通过**：契约完全闭合（dataQuery 10 handler/9 路由，dataWrite 20/20，ocrService 3/3，conversationAI 14 工具）· 前端零直接库操作 · 无死代码（utils/handler 引用计数全非零）· WXML 事件绑定全闭合 · 核心集合 `_openid` 全覆盖（含变量形式）· 无敏感字段落库 · AI 审查链 7 段完整 · reportPdf 健壮（owner 鉴权经 _openid、错误不泄栈、createLayout 每次独立会话防并发污染；已完整接入前端：`pages/report/index.js:535 onExportPdf` + apiClient `generateReportPdf` 路由 + cloudbaserc 配置）。

**审计可复用方法**：① jest 覆盖率须显式排除 _shared 副本 ② 契约闭合=前端 DIRECT_FN key ↔ 云函数 handler 导出双向比对 ③ WXML `bind*/catch*` 提取 ↔ JS 方法名 grep 比对 ④ 静默吞错扫描 pattern：`.catch(() => ({data:[]}))` / `.catch(function(){})`

## 冗余文件扫描：产出 redundancy-scan-20260911.html

只读扫描，未删任何文件。**核心结论：版本库 1738 个跟踪文件里 1321 个（76%）是 AI-IDE 规则镜像**，业务代码仅 139 个。

**关键事实（后续复查直接引用）**
- 项目有 **22 个隐藏工具目录**。同一套 CloudBase 规则（根 `rules/` 100 文件，唯一真源）被复制到 9 处：`.agent/rules`(99) `.clinerules`(100) `.cursor/rules`(100, .mdc) `.kiro/steering`(99) `.qoder/rules`(100) `.windsurf/rules`(100) `.trae/rules`(133) `.codebuddy/rules/tcb/rules`(191) `rules`(100)，内容 md5 逐字节相同。
- 技能包重复：`.trae/skills`(428 跟踪) vs `.codebuddy/skills`(962，41MB，未跟踪) vs `.claude/skills`(42)。`.trae/skills` 内含大体积第三方包（腾讯云CloudBase 717K/skyline 465K/微信小程序框架 311K/Impeccable/darwin-skill/TAPD）。本地镜像目录合计 ~14.5MB。
- 全项目内容重复文件 **1207 个**。`.comate/.roo/.lingma` 各有拼错文件名 `cloudbaase-rules.md`（双 a），三份 md5 相同 `ccf79e94…`。
- 一次性产物**已被跟踪**：`.trae/_count.ps1` `_count2.ps1` `_lines.txt`；根目录 `audit-report.html`(27K,8/9)、`ui-audit-report.html`(43K,8/9)；`.trae/architecture-review-*.html` ×3（7月）。
- 空目录 ×4：`docs/superpowers/{plans,specs}`（整个 docs/superpowers 空壳）、`specs/report-unification-20260906`、`miniprogram/miniprogram_npm`。
- 本地磁盘可回收：`.codebuddy/skills/product-clause-import/node_modules` 34MB + `.codegraph/` 3.8MB + `scripts/poc/` 5.3MB。**勿删** `cloudfunctions/reportPdf/fonts/`（8MB，PDF 运行时依赖）。
- 惯例偏差：`.claude/settings.local.json` 与 `.workbuddy/mcp.json` 被跟踪（后者无密钥；前者带 .local 惯例应不入库）。

**业务代码干净（已交叉验证）**：前端 utils/components 零死文件、云函数 32 个非入口模块零孤儿、`_shared` 主源全被引用、3 页面全注册、6 组件全被 usingComponents 引用、86 测试无孤儿、备份命名文件零命中、跟踪配置无密钥泄露。

**扫描方法（可复用）**
- 零引用判定要**排除 node_modules** 后建全局 require basename 集合再比对；逐文件递归 grep 太慢（>6min）会超时。
- 同一消息内发多条 Bash 会**并行竞态**——共享临时文件（如 /tmp/xxx）时后发命令可能读到未生成的文件，导致全量误报。需串行或各命令自建文件。
- 复杂 node/python 逻辑写到临时脚本文件再跑，别用 `-e` 单行（Windows bash 转义易崩）。
- 组件注册检查：`usingComponents` 值形如 `/components/x/index` 或 `../x/index`，取 basename 前要先剥路径（否则把 `index` 当组件名 → 全部误报未注册）。
